import 'package:flutter/material.dart';

class HomeAuthCubit extends StatelessWidget {
  const HomeAuthCubit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:  Text('Welcome to Home page Ypu are Authenticated'),
      ),
    );
  }
}