import 'package:flutter/material.dart';

class SimplrCartPage extends StatelessWidget {
  const SimplrCartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hello from cart page'),
    );
  }
}